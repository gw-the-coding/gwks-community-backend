spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:gwks-community
    username: sa
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true
  h2:
    console:
      enabled: true
server:
  port: 8080
  servlet:
    context-path: /api/v1
jwt:
  secretKey: GWKS-CHAM-NEN-MA-UEM
  encryptString: gwks-cham-nen-ma-uem-churchinyoungerpeople
